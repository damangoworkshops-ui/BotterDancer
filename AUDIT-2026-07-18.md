# Workflow-Audit Produktionsphase 1 — Zwischenstand 2026-07-18

> Multi-Agent-Audit (5 Inventur-Agenten über C:\ComfyUI, C:\GVHMR, tools\, workflows\, Prozesskette;
> 66 Befunde, davon 14 adversarial verifiziert — 11 CONFIRMED, 2 REFUTED, 1 Verifier ausgefallen).
> Bezugsdokumente: REVIEW-SPIKE.md (10.07.), GO-CHECKLIST.md (10.07.). Letzte Arbeit auf der Platte: 10.07. 21:46.

## Kurzfassung — wo es in Phase 1 hakt

Die Kette selbst ist **funktional validiert** (Final 81 Frames, Draft-Lane 41 s, RIFE 32 fps, Kamera 0–120° + 180° v3),
aber sie ist **zu 100 % Handarbeit** und hat **vier stille Fallen**, die beim Wiedereinstieg nach 8 Tagen Pause zuschlagen:

1. **RIFE rendert stumm den alten Clip** (hartkodiertes `botter_final_00001.mp4` aus `input\` — jeder neue Final heißt `_00002`).
2. **Die Kamera-Lane hat einen Timing-Bug**: 150 Pose-Frames @30 fps, Workflows laden die ersten 81 → nur ~2,7 s Choreo in ~53 % Zeitlupe.
3. **cam60/90/120-Workflows zeigen auf veraltete Pose-Daten** (vor dem Facing-/Gesichts-Fix vom 10.07. 21:28).
4. **Kein Orchestrator, kein Git, kein QA-Gate** — jeder Schritt ist manuelles JSON-Posten + Pfad-Raten.

---

## A. Verifizierte Befunde (CONFIRMED, priorisiert)

### A1 [HIGH] GVHMR-Patches nur als unkommittierte Working-Tree-Änderungen
- `git status` in C:\GVHMR: 22 modifizierte Dateien (demo.py + 21 hmr4d-Dateien), **kein** Commit, kein Branch, kein Stash.
- Ein `git checkout .`/`stash`-Verlust zerstört Stage 2 komplett. `patch_pytorch3d_optional.py` regeneriert nur die hmr4d-Teile (ROOT=hmr4d), **nicht** den manuellen demo.py-Patch.
- ✅ **Heute gesichert:** `tools\gvhmr_botterdancer.patch` (21 KB, voller `git diff`) + `tools\gvhmr_requirements_win.txt` (untracked File).
- **Offen:** Im GVHMR-Repo lokalen Branch `botterdancer` anlegen und committen (2 Minuten, macht den Patch-Export redundant).

### A2 [HIGH] Kamera-Lane: kein fps-Resampling → Zeitlupe + abgeschnittene Choreo
- Alle 7 cam-Workflows: `VHS_LoadImagesPath image_load_cap=81, select_every_nth=1` auf Ordner mit je **150 PNGs @29,97 fps** (GVHMR lief auf dem Original).
- Ergebnis: nur die ersten 81/150 Frames (~2,7 s von 5 s), abgespielt @16 fps = Faktor ~0,53 Echtzeit. final_v2 nutzt dagegen korrekt 80 Frames @16 fps (volle 5 s, via `save_pose_81.json force_rate=16`).
- Die `_comment`s behaupten fälschlich "identisch zu wan_animate_final_v2".
- **Fix:** `--target-fps 16` (Akkumulator-Auswahl, 150→80 Frames) in `reproject_camera.py` — danach cam-Ordner regenerieren. Übergangslösung: `select_every_nth=2` (≈15 fps, leichter Drift).

### A3 [HIGH] Kein Orchestrierungsskript — Phase 1 ist 100 % Handarbeit
- Kein `run_*.ps1`/`pipeline.py`/Makefile; Kette existiert nur als Prosa in GO-CHECKLIST.md. "Neuestes JSON" wird per `Sort LastWriteTime` geraten statt aus der `/history`-API gelesen; Output→Input-Kopien per Hand.
- **Fix (größter Hebel):** `run_phase1.ps1/py`: Workflow posten → `/history` pollen → echte Output-Namen übernehmen → filter_pose_v2 → ffprobe-Gate → RIFE mit konkretem Dateinamen (JSON vor dem POST patchen). Workflow-JSONs als Templates mit Platzhaltern.

### A4 [HIGH] RIFE-Falle: läuft stumm auf dem 8 Tage alten Clip
- `rife_32fps.json` lädt hartkodiert `botter_final_00001.mp4` aus `C:\ComfyUI\input\` (Handkopie vom 10.07., SHA256-identisch mit output-Version).
- Jeder **neue** Final-Render bekommt vom VHS-Counter `_00002` → RIFE läuft fehlerfrei durch, aber auf dem alten Content. Einzige **stille** Fehlklasse der Kette (alles andere schlägt laut fehl).
- **Fix sofort:** vor dem nächsten Final-Render die stale Kopie `input\botter_final_00001.mp4` umbenennen; mittelfristig Dateiname als Parameter (siehe A3).

### A5 [HIGH, Mechanismus bestätigt / formaler Verify ausgefallen] start_comfy.ps1 vernichtet das Benchmark-Log
- Z.8 nutzt cmd-Redirect `>` (Overwrite) auf `C:\ComfyUI\comfyui_utf8.log` — der allererste Schritt jeder Wiederaufnahme löscht die letzte Benchmark-Evidenz. Das Log enthält nur noch die Cam-Session (~15,72–15,75 s/it @20 Steps, ~318 s/Run — deutlich über der 8–10-Erwartung aus GO-CHECKLIST!).
- ✅ **Heute gesichert:** `logs\2026-07-10_cam_session.log`.
- **Offen:** start_comfy.ps1 auf datumsgestempeltes Log oder Append umstellen. (Der zugehörige Verifikations-Agent fiel wegen Session-Limit aus; der `>`-Mechanismus ist aber unabhängig vom tools-audit bestätigt.)

### A6 [MED] rife426.pth liegt weiterhin neben rife417.pth
- rife417.pth (20,5 MiB, validiert) + rife426.pth (23,5 MiB, Arch-Mismatch/"teacher"-Keys) beide in `ComfyUI-Frame-Interpolation\ckpts\rife\`.
- **Wichtig:** Umbenennen/Löschen ist **wirkungslos** — das Node-Dropdown ist hartkodiert (`vfi_models\rife\__init__.py` Z.13–14, CKPT_NAME_VER_DICT) und lädt die Datei bei Auswahl **automatisch neu herunter**. Wirksam ist nur Doku (dieser Eintrag) oder das Auskommentieren der Dict-Zeile.
- Zusatz: der `_comment` in `rife_32fps.json` empfiehlt noch fälschlich rife426 → korrigieren.

### A7 [MED] 80 vs. 81 Frames: dokumentiert-erwartet, ein Detail offen
- `botter_pose_center_v2` = exakt 80 PNGs; der Driver liefert bei force_rate=16 trotz cap 81 nur 80 Frames ("real ~80 verfügbar" steht im save_pose_81-Kommentar). Final-Output hat per ffprobe exakt 81 Frames — Erfolgskriterium erfüllt.
- **Offen (klein):** Wie WanAnimateToVideo den 81. Latent-Frame ohne Pose-Guidance füllt, ist unbelegt → letzten Frame des nächsten Finals einmal bewusst prüfen oder length=80 setzen.
- Die These "Frame 0 = Referenz erklärt 80+1" ist **falsch** — die Referenz läuft separat über reference_image und wird per TrimVideoLatent entfernt.

### A8 [MED] Kamera-Aufrufkette nirgends dokumentiert
- `grep reproject` über alle Projekt-MDs: 0 Treffer. GO-CHECKLIST/REVIEW-SPIKE entstanden VOR dem Kamera-Feature. Interpreter-Pfad steht nirgends.
- **Runbook-Eintrag (der fehlende Fünfzeiler):**
  ```powershell
  C:\GVHMR\.venv\Scripts\python.exe C:\Users\chris\Documents\BotterDancer\tools\reproject_camera.py `
    --src C:\GVHMR\outputs\demo\botter_drive_center\hmr4d_results.pt `
    --outdir C:\ComfyUI\input\botter_pose_camNN --azimuth NN
  # danach wan_animate_camNN.json an POST /prompt (konsumiert genau dieses Verzeichnis)
  ```
- Achtung: das venv heißt **`.venv`**, nicht `venv`.

### A9 [MED] sys.path-Hack auf ComfyUI-Node-Interna, ungepinnt
- `filter_pose_v2.py` Z.19–20 und `reproject_camera.py` Z.39–40/49 importieren `custom_controlnet_aux.dwpose` direkt aus dem Node-Checkout. Kein Vendoring, kein Pin, kein Assert — ein stilles Node-Update ändert das Pose-Rendering **beider** Stages.
- **Hiermit gepinnt (Doku):** comfyui_controlnet_aux stand am 18.07.2026 auf Commit `e8b689a513c3e6b63edc44066560ca5919c0576e`. Vor jedem Node-Update: Reproject-/Filter-Smoke-Test.

### A10 [MED] filter_pose_v2.py: kein `--fps`, fps fehlt in meta.json
- `--src/--outdir/--pad-bottom/--max-gap/--max-jump/--lost-fallback/--keep-all` vorhanden; fps steckt implizit im vorgelagerten `save_pose_81.json` (force_rate=16). Für vollwertige Repro-Sidecars fps in meta.json aufnehmen.

### A11 [LOW] Startzustand heute: GPU frei (1,3/97,9 GiB), ComfyUI läuft NICHT
- Port 8188 zu, kein Compute-Prozess, kein Ollama. GO-CHECKLIST Punkt 0 ist aktuell gegenstandslos — idealer Startzustand, aber jeder POST schlägt fehl, bis `start_comfy.ps1` läuft (vorher A5 fixen!).

## B. Widerlegte Befunde (REFUTED — wichtig gegen Fehlarbeit)

1. **"Alle Ergebnisse des GO-Abends sind unverschriftlicht"** — FALSCH. Die Auto-Memory-Datei (botterdancer-konzept.md, 10.07. 22:19) dokumentiert Dilution-Klärung, Final-Erfolg + 15,78 s/it, Draft-Qualitätsurteil und die komplette cam180-v1/v2/v3-Geschichte. Richtig bleibt nur: die **Projekt**-MDs (GO-CHECKLIST-Checkboxen, Z.34 "LoRA liegt NICHT auf Disk") sind stale → Übertrag ist Kosmetik, kein Wissensverlust.
2. **"reproject_camera.py braucht einen ffmpeg/Video-Schritt"** — FALSCH als Gap. Es wird **kein** pose_video mehr erzeugt: alle Produktions-Workflows laden PNG-Sequenzen direkt via VHS_LoadImagesPath (dokumentierte Verlustfrei-Entscheidung, REVIEW-SPIKE Z.20). Der alte REVIEW-SPIKE-Punkt Z.19 ("Skript besitzt den ffmpeg-Schritt") ist durch die PNG-Lösung **obsolet**. Einziger Rest-Handgriff: `--outdir` muss unter `C:\ComfyUI\input\` liegen.

## C. Wichtige unverifizierte Befunde (Inventur, plausibel, nicht adversarial geprüft)

- **[MED] cam90.json zeigt auf veraltetes `botter_pose_cam90`** (56/150 Frames weichen von `_v2` ab); **cam60/cam120 wurden nie mit dem Facing-Fix regeneriert** (liegen im Winkel-Übergangsbereich, wo der Fix am meisten wirkt); cam0 ist bitidentisch (unkritisch). → Nach A2-Fix ohnehin alles neu generieren.
- **[MED] pytorch3d-Shim wird von setup_gvhmr.ps1 nicht wiederhergestellt** — venv-Rebuild würde das Kamera-Feature still zerstören. ✅ Quellkopie heute gesichert: `tools\pytorch3d_shim\pytorch3d\` (16 Dateien). Offen: Kopier-Schritt in setup_gvhmr.ps1.
- **[MED] Auto-QA-Gates existieren nicht als Code** (SSIM-Leak-Check, Identity-Hold, nvidia-smi-Admission, ffprobe-Assert, meta.json-counts-Assert). Minimal-Set < 100 Zeilen, gehört in den Orchestrator (A3).
- **[MED] Artefakt-Streuung über 4 Orte, Single-Copies unversioniert.** ✅ Heute gespiegelt nach `assets\`: hmr4d_results.pt, botter_pose81_00001.json, creature_ref.png, creature_ref_back.png, botter_drive_center.mp4, botter_pose_center_v2.meta.json. Offen: `git init` im Projekt (RESEARCH.md verweist auf eine Git-History, die es nie gab).
- **[MED] Verlorene Verifikations-Skripte:** `scratchpad/test_camera_axes.py` und `test_facing_z.py` (zweimal Vorzeichenfehler gefunden!) existieren nicht mehr → als `tools\tests\` rekonstruieren, sonst ist nach jedem Update keine schnelle Re-Verifikation möglich.
- **[LOW]** Kommentar-Drift: alle 7 cam-Workflows tragen den Copy-Paste-Kommentar "90 Grad Azimut, Seitenansicht" (6× falsch). VHS_VideoCombine ohne crf/pix_fmt/save_metadata (Defaults betten den Workflow in die MP4-Metadaten ein). setup_gvhmr.ps1 pinnt torch nicht. EMA-Reseed nach fallback-largest ist halbherzig (Ein-Zeilen-Fix: `ema = a`). `creature_ref_back.png` ist nur 16 KB — bekanntes Qualitätsproblem der v3-Referenz (nächster Schritt laut Notizen: saubere Flux-Rück-Referenz).
- **[LOW] cam180 v1/v2 sind abgelöste Iterationsstufen** (Front-Referenz auf Rückansicht) → als deprecated markieren/archivieren, nur v3 ist aktiv.

## D. Was fertig und validiert ist (kein Handlungsbedarf)

- Alle Modelle/Nodes/Checkpoints auf Disk und korrekt (Wan2.2-Animate 32,18 GiB; lightx2v-LoRA 738 MB, exakt im Draft-Workflow verkabelt; rife417; alle 4 Node-Packs).
- Die 6 Sofort-Fixes aus REVIEW-SPIKE (TrimVideoLatent, 512x768, length 81, PNG-Sequenz, crop=none, steps 20) sind in **allen** Produktions-Workflows konsistent drin — keine Regression auf Node-Ebene. rerun_dilution weicht **absichtlich** ab (Kontrollexperiment).
- filter_pose_v2.py: Tracker, Gap-Interpolation, Zähler, OUTDIR-Clearing, Sidecar — sauber umgesetzt. reproject_camera.py: voll parametrisiert, facing_z-Fix drin, floor_y dokumentiert.
- GVHMR-Stack komplett funktional (Python 3.10.20, torch 2.11.0+cu128, CUDA ok, beide demo.py-Patches verifiziert); statische Analyse: reproject-Aufruf funktioniert heute noch.
- GO-CHECKLIST Punkte 0–4 + 5a/5b sind **de facto erledigt** (Datei-Evidenz) — nur die Checkboxen lügen. Wirklich offen: 5c (Benchmark-Abschnitt in RESEARCH.md, unverändert seit 04.07.).

## E. Empfohlene Reihenfolge für die nächste Session

1. **Sofort-Hygiene (15 min, ohne GPU):** stale `input\botter_final_00001.mp4` umbenennen (A4) · start_comfy.ps1-Log datumsstempeln (A5) · rife-Kommentar + cam-Kommentare korrigieren (A6/C) · GO-CHECKLIST-Checkboxen/Z.34 nachziehen · GVHMR-Branch committen (A1).
2. **Kamera-Lane reparieren:** `--target-fps 16` in reproject_camera.py (A2) → alle cam-Ordner als `_v3` regenerieren → Workflows umziehen (löst zugleich C-stale-Punkte).
3. **`run_phase1`-Orchestrator (A3) mit Minimal-QA-Gates (C):** der größte Hebel gegen "es hakt" — macht A4/A6-Fallen strukturell unmöglich.
4. **Danach Inhaltliches:** saubere Flux-Rück-Referenz für 180°, Room-Keeper, Full-Crew (siehe REVIEW-SPIKE Features).
5. Optional: `git init` + Erstcommit des Projekts (inkl. der heute gesicherten assets\/logs\/patches).

## F. Heute gesicherte Artefakte (dieses Audit)

| Datei | Quelle | Zweck |
|---|---|---|
| `logs\2026-07-10_cam_session.log` | C:\ComfyUI\comfyui_utf8.log | Benchmark-Evidenz (15,7 s/it) vor Überschreiben gerettet |
| `tools\gvhmr_botterdancer.patch` | `git -C C:\GVHMR diff` | Alle 22 unkommittierten GVHMR-Patches |
| `tools\gvhmr_requirements_win.txt` | C:\GVHMR (untracked) | Windows-Requirements |
| `tools\pytorch3d_shim\pytorch3d\` | GVHMR-venv site-packages | Shim übersteht venv-Rebuild |
| `assets\` (6 Dateien) | ComfyUI input/output, GVHMR outputs | Repro-kritische Single-Copies gespiegelt |

## G. Ungeprüft / Einschränkungen (ehrliche Restfläche)

- **Nicht geprüft:** wie die bisherigen cam-Renders visuell aussehen (A2-Bewertung ist arithmetisch, nicht visuell); ob rife426 wirklich defekt ist (nur Notizenlage); WanAnimateToVideo-Padding bei 80/81 (A7); alles was GPU-Ausführung bräuchte (Audit war strikt read-only).
- **Verifier ausgefallen** (Session-Limit): A5 formal unbestätigt, Mechanismus aber doppelt belegt.
- **Ungeklärt: `tools\dashboard.py`** (20 KB) ist HEUTE 14:54 **während des Audits** entstanden — ein Monitoring-HUD (Port 8189: nvidia-smi, /queue, /history, Aktionen /free, /interrupt, start_comfy, ollama stop). Es stammt nicht aus der dokumentierten Projekthistorie; vermutlich hat es ein parallel laufender Agent/eine parallele Session geschrieben (das Audit selbst hatte Read-only-Vorgabe). **Bitte prüfen und entscheiden, ob es bleiben soll** — es orchestriert nichts, ist also kein Ersatz für A3.
